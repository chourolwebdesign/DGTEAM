import type { Metadata } from "next";
import { Phone, Mail, MapPin, MessageCircle } from "lucide-react";
import { ContactForm } from "@/components/forms/ContactForm";
import { Button } from "@/components/ui/Button";
import { site } from "@/content/site";
export const metadata:Metadata={title:"Kontakt",description:"DG Team telefonisch, per WhatsApp, E-Mail oder Kontaktformular erreichen.",alternates:{canonical:"/kontakt"}};
export default function ContactPage(){return <><section className="page-hero"><div className="container"><span className="eyebrow">Persönlicher Kontakt</span><h1>Lieber direkt sprechen? Gerne.</h1><p className="lead">Rufen Sie uns an, schreiben Sie per WhatsApp oder nutzen Sie das Formular. Während unserer Geschäftszeiten sind wir persönlich für Sie erreichbar.</p><div className="hero-actions"><Button href={site.phoneHref}><Phone size={18}/> Direkt anrufen</Button><Button href={site.whatsapp} variant="secondary"><MessageCircle size={18}/> WhatsApp</Button><Button href="/angebot" variant="secondary">Angebot anfragen</Button></div></div></section><section className="section"><div className="container quote-shell"><aside className="quote-aside"><h2>Kontaktdaten</h2><div className="audience-list"><div className="audience"><Phone/><a href={site.phoneHref}>{site.phone}</a></div><div className="audience"><Mail/><a href={`mailto:${site.email}`}>{site.email}</a></div><div className="audience"><MapPin/><span>{site.address.street}<br/>{site.address.postalCode} {site.address.city}</span></div></div><p className="lead">{site.hours}</p><div className="notice">Die Besichtigung selbst kann kostenfrei sein. Je nach Entfernung kann eine kleine, vorab transparent kommunizierte Anfahrtspauschale anfallen.</div></aside><ContactForm/></div></section><section className="section" style={{background:'#0d0d0d'}}><div className="container split-grid"><div><span className="eyebrow">Anfahrt</span><h2 className="display">Firmensitz in Krauchenwies.</h2><p className="lead">Die Adresse ist auch ohne externen Kartendienst vollständig verfügbar.</p></div><div className="panel" style={{padding:'3rem'}}><MapPin className="red" size={32}/><h3>{site.address.street}<br/>{site.address.postalCode} {site.address.city}</h3><p className="lead">Eine interaktive Karte wird aus Datenschutzgründen nicht automatisch geladen.</p><a className="button button-secondary" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${site.address.street}, ${site.address.postalCode} ${site.address.city}`)}`} target="_blank" rel="noreferrer">Route extern planen</a></div></div></section></>}
